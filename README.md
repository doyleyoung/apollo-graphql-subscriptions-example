# ApolloClient GraphQL Issue

ApolloClient fails to provide subscription hook "subscribeToMore".

## Start server

In a terminal do:

```bash
cd server-app
yarn start
```

In another terminal

```bash
cd client-app
yarn start
```
